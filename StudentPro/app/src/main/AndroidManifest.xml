<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="studentpro.upb.edu.co.studentpro">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Ingresar"
            android:label="Ingresar" />
        <activity
            android:name=".Registrar"
            android:label="Registrar" />
        <activity
            android:name=".SolicitarValoracion"
            android:label="SolicitarValoracion " />
        <activity android:name=".EstablecerHorario" />
        <activity android:name=".SolicitarServicio" />
        <activity android:name=".Detalles" />
        <activity android:name=".ValidarPago" />
        <activity android:name=".ValidarClase" />
        <activity android:name=".RegistrarDocente" />
        <activity android:name=".RegistrarPadre" />
    </application>

</manifest>